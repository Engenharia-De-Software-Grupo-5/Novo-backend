model Condominiums {
  id          String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name        String       @unique
  description String?
  adress      Adresses     @relation(fields: [adressId], references: [id])
  adressId    String       @db.Uuid
  properties  Properties[]
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
  deletedAt   DateTime?

  @@map("condominiums")
}

model Adresses {
  id           String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  zip          String
  street       String
  neighborhood String
  city         String
  uf           String
  number       Int
  complement   String?
  createdAt    DateTime       @default(now())
  updatedAt    DateTime       @updatedAt
  deletedAt    DateTime?
  condominiums Condominiums[]

  @@map("adresses")
}

model Properties {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  identifier        String            @unique
  adress            String
  unityNumber       String
  unityType         UnityType         @default(APARTMENT)
  block             String?
  floor             Int?
  totalArea         Float?
  propertySituation PropertySituation @default(ACTIVE)
  observations      String?
  createdAt         DateTime          @default(now())
  updatedAt         DateTime          @updatedAt
  deletedAt         DateTime?
  condominium       Condominiums?     @relation(fields: [condominiumId], references: [id])
  condominiumId     String?           @db.Uuid

  @@map("properties")
}

enum UnityType {
  HOUSE
  APARTMENT
  COMERCIAL_ROOM
}

enum PropertySituation {
  ACTIVE
  INACTIVE
  UNAVAILABLE
  MAINTENANCE
}
